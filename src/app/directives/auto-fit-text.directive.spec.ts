import { AutoFitTextDirective } from './auto-fit-text.directive';

describe('AutoFitTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFitTextDirective();
    expect(directive).toBeTruthy();
  });
});
